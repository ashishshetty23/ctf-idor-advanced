<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>CTF IDOR - Home</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body class="page page-home">
    <main class="container">
      <% if (user) { %>
        <p class="muted">Logged in as <strong><%= user.username %></strong></p>
        <form method="post" action="/logout"><button class="btn" type="submit">Logout</button></form>
        <p><a class="link" href="/my-invoices">My Invoices</a></p>
        <p class="hint">DevHint: There's an API <code>/api/max-invoice</code> that returns the highest invoice id.</p>
      <% } else { %>
        <p>You are not logged in. <a class="link" href="/login">Login</a></p>
      <% } %>
    </main>
  </body>
</html>
